import { ValueEqualsDirective } from './value-equals.directive';

describe('ValueEqualsDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueEqualsDirective();
    expect(directive).toBeTruthy();
  });
});
