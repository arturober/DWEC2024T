import { OneCheckedDirective } from './one-checked.directive';

describe('OneCheckedDirective', () => {
  it('should create an instance', () => {
    const directive = new OneCheckedDirective();
    expect(directive).toBeTruthy();
  });
});
